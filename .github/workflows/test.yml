name: Node.js CI
on:
  push:
    branches:
      - main
      - feature/*
  pull_request:
    branches:
      - main
      - develop
      - feature/*

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Use Node.JS 20.x
        uses: actions/setup-node@v2
        with:
          node-version: 20.x

      - run: cd TP_DOCKER_3 && npm install

      - run: cd TP_DOCKER_3 && npm run docker

  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Use Node.JS 14.x
        uses: actions/setup-node@v2
        with:
          node-version: 14.x

      - run: cd TP_DOCKER_3 && npm run lint
